@import './theme'
@import '~vuetify/src/stylus/app'
@import './transitions'

.no-margin {
  margin: 0;
}

$anime := {
    padding: 15px
    width: 150px
    img-height: 210px
    img-border: 4px
    margin: 2px
}

.application.theme--dark {
  h1, h2, h3, h4, h5, h6 {
    color: #D3D3D3;
  }
}

a, .list__tile--active {
  color: inherit;
}